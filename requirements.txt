# Core blockchain & compilation
web3==6.6.0

# GUI
# (tkinter is part of the standard library; no PyPI package)

# Email & CSV handling are stdlib, so no listing needed

# If you deploy from Python:
python-dotenv==1.0.0

# Linting / quality (optional)
flake8==6.1.0
black==24.1.0
isort==5.12.0
